[tox]

envlist = py27


[testenv]

deps =
    nose
    coverage
    pep8
    pyflakes

## test helpers
#    mock

commands =
    coverage erase
    coverage run {envbindir}/nosetests
    coverage report --show-missing --include=replay*
    pyflakes replay
    pep8 replay
